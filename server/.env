# PostgreSQL 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tandem
DB_USER=postgres
DB_PASSWORD=password

# 服务器配置
SERVER_PORT=3001

# API 密钥配置 (用于生成 Stream URL 的安全 Key)
API_KEY_SECRET=tandem-timeseries-secret-2024

# 注意：InfluxDB 配置现在存储在数据库中 (influx_configs 表)
# 每个模型可以有自己独立的 InfluxDB 配置，通过前端配置面板设置
