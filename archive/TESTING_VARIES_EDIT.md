# 批量编辑 "多个" 值功能测试

## 功能说明
当选择多个对象，且某个属性在这些对象中有不同的值时，该属性会显示为 "多个"（中文）或 "Varies"（英文）。现在这些字段也支持编辑，编辑后所有选中的对象将统一更新为新值。

## 视觉提示

### 普通字段
- 颜色: 白色 (#eee)
- 鼠标悬停: 背景略亮，边框变亮

### "多个" 字段  
- 颜色: 浅蓝色 (#9cdcfe)
- 字体: 斜体
- 边框: 深灰色 (#4a4a4a)
- 鼠标悬停: 
  - 边框变蓝色 (#0078d4)
  - 颜色变亮 (#6cb6ff)
  - 显示提示: "点击编辑所有选中对象"

## 编辑行为

### 1. 点击"多个"字段
- 进入编辑模式
- **输入框为空**（而不是显示 "__VARIES__"）
- 光标自动聚焦到输入框

### 2. 输入新值
- 在空白输入框中输入统一的新值
- 例如：输入 "F2" 作为楼层

### 3. 保存
- 按 Enter 键确认
- 或点击其他区域（失焦）自动保存

### 4. 批量更新
- 所有选中对象的该属性更新为新值
- 控制台显示批量更新进度和结果

## 测试场景

### 场景1: 统一楼层编号

1. **选择3个不同楼层的资产**
   - 资产A: 楼层 = "F1"
   - 资产B: 楼层 = "F2"  
   - 资产C: 楼层 = "F3"

2. **查看右侧属性面板**
   - 楼层字段显示: "多个" (浅蓝色，斜体)
   - 鼠标悬停显示: "点击编辑所有选中对象"

3. **点击楼层字段**
   - 输入框为空
   - 光标已聚焦

4. **输入 "F2"**
   - 按 Enter

5. **验证结果**
   - 控制台显示: `🔢 批量编辑模式: 将更新 3 个对象`
   - 控制台显示: `✅ 批量更新完成: 成功 3, 失败 0`
   - 楼层字段现在显示: "F2" (普通白色文本)
   - 所有3个资产的楼层都变成 "F2"

### 场景2: 统一分类编码

1. **选择多个资产，分类编码不同**
2. **分类编码显示 "多个"**
3. **点击编辑，输入统一的分类编码**
4. **确认后所有资产分类编码统一**

### 场景3: ESC 取消编辑

1. **点击 "多个" 字段**
2. **开始输入新值**
3. **按 ESC 键**
4. **编辑取消，字段恢复显示 "多个"**

### 场景4: 混合编辑

1. **选择5个资产**
2. **名称字段: 相同值 "Asset-A"** → 显示 "Asset-A"（普通）
3. **楼层字段: 不同值** → 显示 "多个"（蓝色斜体）
4. **编辑楼层为 "F1"** → 所有5个资产楼层统一
5. **楼层字段现在显示 "F1"**（普通）

## 控制台日志示例

```
字段 level 更新为: F2
🔢 批量编辑模式: 将更新 3 个对象
🔄 正在更新资产: MC001
✅ 资产 MC001 更新成功
🔄 正在更新资产: MC002
✅ 资产 MC002 更新成功
🔄 正在更新资产: MC003
✅ 资产 MC003 更新成功
✅ 批量更新完成: 成功 3, 失败 0
📝 App.vue 收到属性变更: level = F2
✅ 已更新 assetList 中 MC001 的 floor
✅ 已更新 assetList 中 MC002 的 floor
✅ 已更新 assetList 中 MC003 的 floor
```

## 关键代码变更

### EditableField.vue
```javascript
// 允许编辑 __VARIES__ 值
const startEdit = async () => {
  if (props.disabled) return;  // 移除了 __VARIES__ 检查
  
  editing.value = true;
  
  // 如果是 VARIES，清空输入框
  if (props.modelValue === '__VARIES__') {
    editValue.value = '';
  } else {
    editValue.value = props.modelValue || '';
  }
  
  await nextTick();
  inputRef.value?.focus();
  inputRef.value?.select();
};
```

### 样式
```css
.val-box.varies {
  color: #9cdcfe;
  font-style: italic;
  border-color: #4a4a4a;
}

.val-box.varies:hover {
  border-color: #0078d4;
  color: #6cb6ff;
}
```

## 预期行为总结

✅ "多个" 字段可点击编辑
✅ 点击后输入框为空（清空 __VARIES__）
✅ 输入新值后批量更新所有选中对象
✅ 视觉提示明显（蓝色斜体）
✅ 鼠标悬停显示提示文本
✅ 支持 Enter 确认和 ESC 取消
